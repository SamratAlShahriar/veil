# example/pubspec.yaml
name: veil_example
description: Example app for the veil package.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  veil:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0